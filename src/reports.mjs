export const escapeHtml=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function reportHtml(r){const e=escapeHtml;return `<!doctype html><html lang="id"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; img-src 'none';"><title>${e(r.title)} — CekNaskah</title><style>body{max-width:960px;margin:45px auto;padding:0 25px;font:16px/1.8 system-ui;color:#234336}h1{font-family:Georgia}small{color:#627968}article{border-top:1px solid #ccc;padding:18px 0}pre{white-space:pre-wrap;overflow-wrap:anywhere;font:inherit;background:#f4f7f2;padding:20px}blockquote{border-left:3px solid #a7bd70;padding-left:20px;margin-left:0} @media print{body{margin:0}article{break-inside:avoid}}</style><h1>CekNaskah · ${e(r.title)}</h1><p>${e(r.createdAt)} · Aplikasi ${e(r.appVersion)} · Status: ${e(r.status)}</p><h2>Kemiripan tekstual: ${r.score===null?'Tidak tersedia':Number(r.score).toFixed(1)+'%'}</h2><p>${e(r.matchedTokens)} token cocok / ${e(r.eligibleTokens)} token diperiksa. Total: ${e(r.totalTokens)}. Minimum: ${e(r.minMatch)} token. Durasi: ${e(r.durationMs)} ms.</p><p>${e(r.policy)}</p><p>Hanya terhadap koleksi lokal. Bukan persentase plagiarisme, deteksi AI, atau skor Turnitin. Sumber dan sitasi perlu ditinjau.</p><h2>Sumber yang diperiksa</h2>${r.used.map(s=>`<p>${e(s.title)} · ${e(s.matchedTokens)} token · versi ${e(s.version)} · ${e(s.parseStatus)}<br><small>SHA-256: ${e(s.hash)}</small></p>`).join('')}<h3>Sumber gagal</h3>${r.failed.map(s=>`<p>${e(s.title)}: ${e(s.error)}</p>`).join('')||'<p>Tidak ada.</p>'}<h3>Pengecualian naskah</h3>${(r.exclusions||[]).map(x=>`<p>Karakter ${e(x.start)}–${e(x.end)}: ${e(r.text.slice(x.start,x.end))}</p>`).join('')||'<p>Tidak ada.</p>'}<h2>Bukti kecocokan</h2>${r.matches.map((m,i)=>`<article><h3>${i+1}. ${e(m.sourceTitle)}</h3><p>Karakter naskah ${e(m.start)}–${e(m.end)}; sumber ${e(m.sourceStart)}–${e(m.sourceEnd)} · ${e(m.words)} token</p><pre>${e(r.text.slice(m.start,m.end))}</pre><blockquote>${e(m.quote)}</blockquote><p>Catatan: ${e((r.notes||[]).find(n=>n.matchId===m.id)?.text||'Belum ada catatan.')}</p></article>`).join('')}<h2>Naskah yang diperiksa</h2><pre>${e(r.text)}</pre><small>SHA-256 naskah: ${e(r.hash)}<br>Versi induk: ${e(r.parentId||'—')} · ID laporan: ${e(r.id)}</small></html>`;}
export function download(content,name,type){const url=URL.createObjectURL(new Blob([content],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.hidden=true;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),10000);}
export function exportReport(r,format){download(format==='html'?reportHtml(r):JSON.stringify(r,null,2),`CekNaskah-${r.id.slice(0,8)}.${format}`,format==='html'?'text/html':'application/json');}
